apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: grafana-admin-credentials
  namespace: monitoring
spec:
  provider: azure
  parameters:
    usePodIdentity: "false"
    clientID: f64c420b-cc5a-45e4-934e-514b0962c461 # Client ID of the Workload Identity (Managed Identity), see $WORKLOAD_IDENTITY_NAME
    keyvaultName: ci-tfs-prod # Name of the key vault, see $KEYVAULT_NAME
    objects:  |
      array:
        - |
          objectName: grafana-admin-user
          objectType: secret
        - |
          objectName: grafana-admin-password
          objectType: secret
    tenantId: 5159fdaa-fc6b-46fc-a28e-8f6a27aa5862 # Tenant ID where the resources resides
  secretObjects:
  - data:
    - key: admin-user
      objectName: grafana-admin-user
    - key: admin-password
      objectName: grafana-admin-password
    secretName: grafana-admin-credentials
    type: Opaque